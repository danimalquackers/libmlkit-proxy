# Enable AndroidX support across the entire build pipeline
android.useAndroidX=true

# Automatically convert legacy support library dependencies to AndroidX variants if needed
android.enableJetifier=true